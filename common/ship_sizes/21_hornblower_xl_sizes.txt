# reloadable with 'reload stats'
# This file is where you start when you want to configure something new, then go to sections

####################
# NOTE FOR MODDERS #
####################
# modifier = { } only takes the following modifiers
# hull, shield & armor add/mult and regen add perc/static
# evasion add/mult
# speed/base speed mult
# weapon damage mult
# upkeep mult
# for other modifiers, use ship_modifier = { }
# use_shipnames_from = other_size # allows a ship size to use names from another size
# text_icon = military_size_1 	# military_size_1 is default, it will use GFX_text_<key> in fleet composition screens
# fleet_view_background = "" 	# ship size key is default, it will use GFX_fleet_view_main_bg_<MaPo>_<fleet_view_background> 
#								for the fleet view background. For minor powers it'll use GFX_fleet_view_main_bg_<fleet_view_background>
####################

@base_operative_officers = 20
@base_officers_escape_perc = 15

@corvette_size_icon_frame = 2
@destroyer_size_icon_frame = 3
@cruiser_size_icon_frame = 4
@battleship_size_icon_frame = 5
@titan_size_icon_frame = 6
@juggernaut_size_icon_frame = 7

########################
########################
####      UFOP      ####
########################
########################

############
#  COMBAT  #
############


neghvar_flagship = {
	formation_priority = @neghvar_formation_priority
	max_speed = @neghvar_speed
	acceleration = 0.17
	rotation_speed = 0.3
	collision_radius = @neghvar_collision_radius
	max_hitpoints = @neghvar_hp
	operative_officers = @base_operative_officers
	officers_escape_perc = @base_officers_escape_perc
	fleet_view_background = "military_neghvar"

	ship_modifier = {
		ship_fire_rate_mult = @no_admiral_fire_rate_mult
		ship_shield_mult = @no_admiral_shield_mult
		ship_shield_damage_mult = @no_admiral_shield_damage_mult
		ship_shield_penetration_mult = @no_admiral_shield_penetration_mult
		ship_tracking_mult = @no_admiral_tracking_mult
		ship_evasion_add = @neghvar_evasion
		#ship_piracy_suppression_add = 160
	}
    
	size_multiplier = 13
	fleet_slot_size = 8
	graphical_culture = no
	
	section_slots = { "mid" = { locator = "part1" }
	"bow" = { locator = "part2" } }
	
	num_target_locators = 2
	is_space_station = no
	icon_frame = @titan_size_icon_frame
	base_buildtime = @neghvar_build_time
	can_have_federation_design = yes
	enable_default_design = no	#if yes, countries will have an auto-generated design at start
	is_designable = no 
	is_civilian = no 
	

	default_behavior = "hrb_artillery"

	prerequisites = { "tech_titan_unlock" }

	potential_country = {
		has_country_id = klingon
	}

	possible_construction = {
		custom_tooltip = {
			is_scope_type = starbase
			has_starbase_building = titan_yards
			fail_text = starbase_titan_yards_trigger
		}
	}

	combat_disengage_chance = 0.75

	class = shipclass_military
	construction_type = starbase_shipyard
	required_component_set = "power_core"
	required_component_set = "ftl_components"
	required_component_set = "thruster_components"
	required_component_set = "sensor_components"
	required_component_set = "combat_computers"

	resources = {
		category = ships
		upkeep = {
			trigger = {
				NOT = {
					ROOT = {
						exists = FROM
						original_owner_uses_artificial_singularities = FROM
					}
				}
			}
			deuterium = @neghvar_upkeep_deuterium
		}
		
		upkeep = {
			trigger = {
				ROOT = {
					exists = FROM
					original_owner_uses_artificial_singularities = FROM
				}
			}
			artificial_singularities = 0
		}

		upkeep = {
			alloys = @neghvar_upkeep_alloys
		}
	}

	min_upgrade_cost = {
		alloys = 1
	}
}
